# Public, build-time setting. Never put secrets in VITE_ variables.
# Includes the /api prefix. Defaults to /api when unset.
VITE_API_BASE_URL=/api

# Keep disabled for local development. The future private server build sets server.
VITE_AUTHENTICATION=local
