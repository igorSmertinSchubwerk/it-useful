spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:it_useful}
    username: ${DB_USER:it_useful}
    password: ${DB_PASSWORD:it_useful_local}
  jpa:
    hibernate:
      ddl-auto: validate
