spring:
  docker:
    compose:
      enabled: false
  datasource:
    # Fail closed if a test forgets to import the container configuration.
    url: jdbc:postgresql://127.0.0.1:1/container-configuration-required
    username: unused
    password: unused
  jpa:
    hibernate:
      ddl-auto: validate

app:
  storage:
    directory: target/test-uploads
